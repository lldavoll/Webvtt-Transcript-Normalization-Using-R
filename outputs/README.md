# Outputs

This directory is used to store cleaned and processed transcript files generated by the pipeline.

The contents of this folder are not included in the repository because they are derived from restricted and non-public data.

To generate outputs locally:

1. Place authorized transcript files in `data/raw/`
2. Run the pipeline:
   ```r
   source("run_pipeline.R")
